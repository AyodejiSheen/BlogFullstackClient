import { createContext } from "react";

export const AuthContext = createContext("");

//to create a context APi to manage the rerendering state that change the navbar when user login and not logged in